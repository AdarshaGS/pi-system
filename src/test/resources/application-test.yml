spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:
  
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
    properties:
      hibernate:
        format_sql: false
        dialect: org.hibernate.dialect.H2Dialect
  
  flyway:
    enabled: false
  
  data:
    redis:
      host: localhost
      port: 6379

jwt:
  secret: test-secret-key-for-testing-purpose-only-minimum-256-bits-required-for-hs256-algorithm
  expiration: 3600000
  refresh-token:
    expiration: 86400000

aa:
  mode: MOCK

server:
  port: 0

logging:
  level:
    root: INFO
    com.stocks: DEBUG
