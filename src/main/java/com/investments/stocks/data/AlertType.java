package com.investments.stocks.data;

public enum AlertType {
    TARGET_PRICE,      // Alert when price reaches target
    PERCENTAGE_UP,     // Alert when price increases by %
    PERCENTAGE_DOWN    // Alert when price decreases by %
}
