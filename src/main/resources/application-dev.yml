logging:
  level:
    root: INFO
    com.stocks: DEBUG
    org.springframework.web: INFO
    org.hibernate.SQL: DEBUG

spring:
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: none  # Changed from 'validate' to allow drift detection
    properties:
      hibernate:
        format_sql: true